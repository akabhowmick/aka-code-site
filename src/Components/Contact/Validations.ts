export const validEmail = (email: string) =>{
  return email.length > 0 && email; 
}

export const validName = (email: string) =>{
  return email.length > 0 && email; 
}

export const validMessage = (email: string) =>{
  return email.length > 0 && email; 
}